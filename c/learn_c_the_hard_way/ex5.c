#include <stdio.h>

int main(int argc, char *argv[]) {
  // argc - the number of arguments the program has received.
  // argv - an array of strings with the values of the characters.
  int distance = 100;

  printf("You are %d miles away.\n", distance);

  return 0;
}
